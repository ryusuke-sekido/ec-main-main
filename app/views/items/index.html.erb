<h1>商品一覧</h1>
<% @items.each do |item| %>
 <span>・<%= item.name %> <%= item.introduction %>
 <%= link_to item_path(item.id) do %>
 <%= image_tag item.image, size: "100x100" %>
 <% end %>
 <%= item.price %>円
 </span>
<% end %>
